pluginManagement {
    val flutterSdkPath =
        run {
            val properties = java.util.Properties()
            file("local.properties").inputStream().use { properties.load(it) }
            val flutterSdkPath = properties.getProperty("flutter.sdk")
            require(flutterSdkPath != null) { "flutter.sdk not set in local.properties" }
            flutterSdkPath
        }

    includeBuild("$flutterSdkPath/packages/flutter_tools/gradle")

    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    id("dev.flutter.flutter-plugin-loader") version "1.0.0"
    id("com.android.application") version "9.0.1" apply false
    id("org.jetbrains.kotlin.android") version "2.3.20" apply false
    // FEATURE (push notifications, 2026-07-05): required for Firebase's
    // Android integration -- reads google-services.json and wires the
    // app's package name to the matching Firebase project at build time.
    // See PUSH_NOTIFICATIONS_SETUP.md.
    id("com.google.gms.google-services") version "4.4.2" apply false
}

include(":app")